[package]
name = "hamheatmap-terrain"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
tiff = { version = "0.11.3", default-features = false, features = ["deflate"] }

